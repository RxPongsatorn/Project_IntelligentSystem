import streamlit as st

# ตั้งค่าหน้าเว็บ
st.title("Explaination of the Project")

# สร้าง Section ต่างๆ เพื่ออธิบาย
st.header("How to Develop")
st.write("""
    1. **เลือกชุดข้อมูล (Selecting Data)** - เราเริ่มต้นด้วยการเลือกชุดข้อมูลที่เกี่ยวข้องกับธุรกิจที่ต้องการ เช่น ชุดข้อมูลการให้สินเชื่อ (Loan Dataset) ซึ่งมีข้อมูลของลูกค้าและจำนวนเงินที่ขอสินเชื่อ
    2. **ทำความสะอาดข้อมูล (Data Preprocessing)** - เราลบค่าที่ขาดหาย (missing values) และจัดการกับข้อมูลที่ไม่เหมาะสม เช่น การแปลงค่าสกุลเงินเป็นตัวเลข
    3. **เลือกคุณสมบัติ (Feature Selection)** - การเลือกคุณสมบัติที่จะใช้ในการสร้างโมเดล เช่น เลือก `customer_income` และ `loan_amnt` สำหรับการทำนาย
    4. **สร้างโมเดล Machine Learning** - เลือกโมเดลที่เหมาะสม (Linear Regression, KNN, K-Means Clustering) เพื่อทำนายผลลัพธ์
    5. **ฝึกฝนและทดสอบโมเดล** - ใช้ข้อมูลฝึกฝน (Training Data) เพื่อสร้างโมเดลและทดสอบโมเดลด้วยข้อมูลทดสอบ (Testing Data)
    6. **วิเคราะห์ผลลัพธ์** - แสดงผลลัพธ์ที่ได้จากโมเดลต่างๆ เช่น KNN, K-Means, และ Linear Regression
""")

st.header("Dataset")
st.write("""
    ชุดข้อมูลที่ใช้ในโปรเจกต์นี้เป็นชุดข้อมูลเกี่ยวกับการให้สินเชื่อ (Loan Dataset) ซึ่งประกอบด้วยข้อมูลต่างๆ เช่น:
    - **loan_amnt**: จำนวนเงินที่ขอสินเชื่อ
    - **customer_income**: รายได้ของลูกค้า
    - และข้อมูลอื่นๆ ที่เกี่ยวข้อง เช่น อายุ, ประวัติการชำระหนี้, และอาชีพ
    
    ข้อมูลที่เราใช้ในการพัฒนาเป็นข้อมูลจริงจากตลาดสินเชื่อที่มีความหลากหลาย
""")

st.header("Theory of Algorithm")
st.write("""
    1. **Linear Regression**:
        - ทฤษฎีพื้นฐาน: ใช้ในการทำนายค่าต่อเนื่อง (Continuous Prediction) เช่น การทำนายจำนวนเงินกู้ตามรายได้ของลูกค้า
        - โมเดลนี้ทำงานโดยการหาความสัมพันธ์เชิงเส้นระหว่างตัวแปรอิสระ (Features) และตัวแปรตาม (Target Variable)
    
    2. **K-Nearest Neighbors (KNN)**:
        - ทฤษฎีพื้นฐาน: เป็นอัลกอริธึมการจัดกลุ่มที่ใช้ข้อมูลจากจุดที่ใกล้เคียงที่สุด (neighbors) เพื่อตัดสินใจในการจัดประเภท
        - ใช้ค่า `K` ที่เป็นจำนวนของจุดข้อมูลที่ใกล้เคียงที่สุดในการทำนาย
    
    3. **K-Means Clustering**:
        - ทฤษฎีพื้นฐาน: อัลกอริธึมการแบ่งกลุ่มข้อมูล (Clustering) โดยการหาจุดศูนย์กลาง (Centroids) ของแต่ละกลุ่ม
        - ใช้เพื่อแบ่งข้อมูลออกเป็น `k` กลุ่มโดยไม่ต้องใช้ข้อมูลป้ายชื่อ (Unsupervised Learning)
""")

st.header("Step to Develop")
st.write("""
    1. **เตรียมชุดข้อมูล**:
        - โหลดข้อมูลจากไฟล์ CSV และทำความสะอาดข้อมูล เช่น การจัดการกับค่าที่ขาดหายและการแปลงข้อมูลที่ไม่เหมาะสม
    2. **เลือกโมเดล**:
        - เลือกอัลกอริธึมที่เหมาะสมตามประเภทของปัญหา เช่น การใช้ Linear Regression สำหรับการทำนายค่า (Regression) หรือ KNN สำหรับการจำแนกประเภท (Classification)
    3. **การฝึกฝนโมเดล**:
        - ใช้ข้อมูลฝึกฝนในการสร้างโมเดล และทดสอบโมเดลด้วยข้อมูลทดสอบ
    4. **การทดสอบและประเมินผล**:
        - ทดสอบโมเดลที่ได้และประเมินผลด้วยเมตริก
    5. **การแสดงผลลัพธ์**:
        - แสดงผลการทำนาย และทำการวิเคราะห์ด้วยกราฟหรือข้อมูลที่มีความหมาย
""")

st.markdown("""
    ## Additional Resources:
    - [Scikit-learn Documentation](https://scikit-learn.org/stable/documentation.html)
    - [Streamlit Documentation](https://docs.streamlit.io/)
""")
